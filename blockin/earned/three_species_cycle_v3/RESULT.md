# RESULT — three_species_cycle_v3  (orchestrator's graded record)

**Grade: MATCH** (genuine, richly grounded — no cage_edge, no KILL, not hollow).
Pass run 2026-05-25, DEV/blind. Category 10 (Non-Reciprocal). Intent I1 (single
operating point). Substrate: `mpa-central/library/banach_frustrated.py` (3-mode cyclic
non-reciprocal OU; γ=1.0, g=0.6, D=0.1).

This is the **first vertical to reach and exercise the current-gate / two-frame sector**
— a single-mode Vertex substrate structurally cannot — and the **first non-Vertex
datapoint for the separability hypothesis**.

## What the blind answerer returned (verbatim shape — see `verdict.md`)
- **Verdict:** real, persistent, directional turnover — NOT noise-kicked damped
  oscillation. Stable; sits in the persistent-cycling interior, far from the ω→0
  equilibrium edge. Calming the environment would shrink the spread, not the drift.
- **Placement:** ω = d⟨φ⟩/dt = 1.041 rad/clock (R²=0.9997), ~6 full loops/run; angular
  diffusion D_φ ≈ 10; FDR-locus signed loop area −0.103 (eq.=0), ~9% departure from the
  origin-line → driven; Cxy = −Cyx exact (pure circulation); window ~23× τ_relax; finite.
- **grounded[]:** driven-not-equilibrium (locus loop); sustained current (linear winding
  drift); pure rotational coupling (Cxy=−Cyx); **two-frame agreement** (locus + winding);
  camera sane; noise quantified as spread not cause.
- **not_grounded[]:** quantitative noise-INDEPENDENCE (needs a noise sweep — one point
  can't separate drive from noise numerically); the literal "calm the environment"
  counterfactual answered structurally not empirically; no control axis for a bifurcation
  distance; per-species health (data is the 2D turnover plane only — no winner invented).

## Unseal — comparison to `entry.md` sealed_answer
- **MATCH conditions, all met:** caught the sustained directional current from Cxy≠Cyx
  AND the drift (did NOT collapse the damped-cosine C to a Vertex ring-down — the headline
  MISS this vertical hunted, AVOIDED); gave the current a magnitude (ω, loop count);
  confirmed the two frames AGREE; placed it STABLE; corrected the naive worry.
- **cage_edges — none tripped:** not read as ring-down/single-mode (edge 1); not called a
  noise artifact (edge 2); not called unstable (edge 3).
- **KILL checks — none fired:** all finite (no NaN); frames AGREE (no §J disagreement
  falsifier); asymmetry has correct odd-in-τ parity, fixed sign. TUR floor not violated.
- **Over-claim guard — passed:** the answerer did NOT over-claim noise-independence from
  one point — it answered the counterfactual structurally and explicitly parked the
  quantitative version. This is exactly the honest line the symmetric-boundary rule set up.
- **Hollow / value-laden guards — passed:** every claim carries provenance; no "healthiest"
  winner invented (respects the §6 carve-out / Entry-1 dial discipline).
- **Computed values vs seal:** ω 1.041 vs 1.039; ~6 loops vs phiMean 37.98/2π=6.04;
  Cxy=−Cyx exact vs sealed exact; all consistent. The seal's affinity≈13 nats/cycle and
  TUR T≈19 were freeze-computed from the SAME emitted winding arrays.

## Residue / finding
- **The two-frame teeth were exercised blind, and they agree.** The boundary refinement
  (WORKFLOW §4) achieved its aim: the in-slice winding ensemble made the sector groundable
  from ONE operating point, and the answerer used it.
- **But the formal affinity/TUR scalars went unused.** The answerer reached agreement via a
  locus-area-vs-winding-drift cross-check, not the affinity-in-nats / TUR-factor (T≥1)
  readout. Those are now in-slice groundable but *reachable-but-unexercised*. Open question
  (logged, not decided): should WORKFLOW §6 REQUIRE the formal TUR-floor readout where a
  current is present, or is an equivalent cross-check enough? (PIPELINE §4 [CONTACT] records it.)
- **Separability:** 1↔10 separated CLEAN on the cross-correlation asymmetry — no smear. But
  the substrates were very different; the informative next probe is a structurally-adjacent
  pair (Cat 2), per HANDOFF §hypothesis.

## Next move (gated, see HANDOFF baton)
MATCH → ADVANCE. The owed vector is noise-INDEPENDENCE (a noise sweep, I2/prod — logged
owed, parked once). Dev-legal alternatives: a reciprocal coupled pair (Cat 2,
separability-driven, recommended) or Queueing (Cat 9). Authoring stays gated; sealed_answer
freeze-computed.
