# Researcher path. Placeholder for the first session.
#
# The researcher path turns raw uploads (CSV, time-series, pre-computed FDR)
# into signed declaration_bundle.json instances. Built second, after the
# curator path lands and after outside research returns concrete picks for
# the bundled-LLM stack and the MCP ecosystem (see
# docs/research-findings.md).
